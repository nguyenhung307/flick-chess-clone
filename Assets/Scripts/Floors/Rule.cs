using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Rule : MonoBehaviour
{
    public bool StartRound1, StartRound2;
    // Start is called before the first frame update
    void Start()
    {
        StartRound1 = false;
        StartRound2 = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
